#include<bits/stdc++.h>
#define ll long long
#define fastio ios::sync_with_stdio(0); cin.tie(0);
using namespace std;



int main() {
    int n,a;
    cin >> n >> a;
    for(int i = 0; i < n; i++) {
        int x;
        cin >> x;
    }

    string s;
    cin >> s;
    for(int i = s.size()-1; i >= 0; i--) {
        cout << s[i];
    }
}